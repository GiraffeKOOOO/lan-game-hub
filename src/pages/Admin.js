import { useContext, useState } from "react";
import "../App.css";

function Admin() {
  // checkAuth()

  // if true log in take user to admin page

  // else redirect to log in page

  return (
    <>
      <h1>Admin page</h1>
    </>
  );
}

export default Admin;
